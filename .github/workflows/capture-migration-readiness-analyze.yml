name: Capture migration readiness analyze failure

on:
  push:
    branches:
      - audit/migration-readiness-inventory

permissions:
  contents: write

jobs:
  capture:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: audit/migration-readiness-inventory

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate Drift code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Capture analyze output
        run: |
          set +e
          flutter analyze > /tmp/flutter-analyze.log 2>&1
          status=$?
          mkdir -p diagnostics
          {
            echo "flutter analyze exit status: $status"
            echo
            cat /tmp/flutter-analyze.log
          } > diagnostics/migration-readiness-analyze.txt

      - name: Commit diagnostic report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add diagnostics/migration-readiness-analyze.txt
          git commit -m "Capture migration readiness analyze failure"
          git push origin HEAD:audit/migration-readiness-inventory
